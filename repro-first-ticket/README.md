# repro-first-ticket (T0-1 incremental)

这个目录用于按 **一次一个文件** 的方式，逐步完成第一个 ticket（T0-1：初始化仓库与基础文件）。

## 为什么现在先补 README
T0-1 的目标不是立刻实现推理逻辑，而是先把项目“可被人接手”的最小工程信息补齐。
在只允许每次改动一个文件的约束下，README 是性价比最高的第一补充：

1. 明确目录用途，避免和主仓库代码混淆。
2. 记录当前阶段目标（T0-1），让后续提交有连续上下文。
3. 给出后续改动策略（每次一文件），降低协作歧义。

## 当前状态
- 已有 `pyproject.toml`（uv 环境入口）。
- 本次新增 `README.md` 作为项目说明。

下一步将继续遵循“每次只改一个文件”。
